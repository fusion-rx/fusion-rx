{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "FsnPackageConfig",
    "description": "JSON Schema for `fsn-package.json` description file",
    "type": "object",
    "properties": {
        "$schema": {
            "type": "string"
        },
        "dest": {
            "type": "string",
            "description": "Destination folder where distributable binaries of the Fusion library are written (default: `dist`)."
        },
        "assets": {
            "type": "array",
            "description": "A list of files which are simply copied into the package."
        },
        "lib": {
            "type": "object",
            "properties": {
                "entryFile": {
                    "description": "Entry file to the public API (default: `src/public_api.ts`)",
                    "type": "string"
                }
            }
        }
    }
}
