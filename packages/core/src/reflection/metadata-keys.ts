/** The metadata key for the name of a class. */
export const CLASS_NAME = 'fusion:classname';

/** The metadata key for class parameter types. */
export const PARAM_TYPES = 'design:paramtypes';

/** The metadata key for the Injectable providedIn value. */
export const PROVIDED_IN = 'fusion:providedin';

/** The metadata key for Injectable dependencies */
export const INJECTED_DEPS = 'fusion:injected';

/** The metadata key for dynamic Injectables */
export const DYNAMIC_INJECTED = 'fusion:injectedynamic';

/** The metadata key for injectable instance metadata. */
export const INJECTABLE_INSTANCE = 'fusion:instance';

/** The metadata key for module imports. */
export const IMPORTS = 'fusion:imports';

/** The metadata key for module exports. */
export const EXPORTS = 'fusion:exports';

/** The metadata key for module providers. */
export const PROVIDERS = 'fusion:providers';

/** The metadata key for module factory providers. */
export const FACTORY_PROVIDER = 'fusion:factoryprovider';

/** The metadata key for module routes. */
export const ROUTES = 'fusion:routes';

/** Metadata key for provider dynamic injections. */
export const INJECT = 'fusion:inject';
